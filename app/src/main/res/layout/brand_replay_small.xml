<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@id/brand_replay_layout"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:layout_centerHorizontal="true"
    android:layout_gravity="center_horizontal">
    <ImageView
        android:id="@id/brand_replay_button"
        android:layout_width="30.0dip"
        android:layout_height="30.0dip"
        android:layout_centerHorizontal="true"
        android:src="@drawable/brand_replay" />
    <TextView
        android:id="@id/brand_replay_text"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/brand_replay_button"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="3.0dip"
        android:text="@string/brand_replay_text"
        android:textColor="#ffffffff"
        android:textSize="12.0sp" />
</RelativeLayout>