<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@id/ll_ad_body_inner"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent">
    <RelativeLayout
        android:id="@id/ll_ad_container"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:minWidth="300.0dip"
        android:minHeight="250.0dip" />
    <ImageView
        android:id="@id/iv_close"
        android:layout_width="48.0dip"
        android:layout_height="48.0dip"
        android:layout_alignTop="@id/ll_ad_container"
        android:layout_alignRight="@id/ll_ad_container"
        android:padding="10.0dip"
        android:src="@drawable/iv_close_icon" />
    <TextView
        android:id="@id/seconds_view"
        android:layout_width="26.0dip"
        android:layout_height="26.0dip"
        android:layout_alignTop="@id/ll_ad_container"
        android:layout_alignRight="@id/ll_ad_container"
        android:layout_margin="10.0dip"
        android:background="@drawable/btn_count_down_style"
        android:gravity="center"
        android:maxLines="1"
        android:shadowColor="#ff000000"
        android:shadowDx="0.0"
        android:shadowDy="1.0"
        android:shadowRadius="3.0"
        android:text="3s"
        android:textColor="#ffffffff"
        android:textSize="12.0sp"
        android:visibility="gone" />
</RelativeLayout>